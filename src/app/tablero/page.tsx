import { EnConstruccion } from "@/components/EnConstruccion";

export default function PaginaTablero() {
  return (
    <EnConstruccion
      fase="Fase 3"
      titulo="Tablero de demanda"
      descripcion="Las solicitudes abiertas ordenadas por cuánta gente las necesita, con buscador por código."
    />
  );
}
